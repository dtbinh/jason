#!/bin/sh

# start a program that enable you to start agents
cd `dirname $0`
. ./saci.env > /dev/null

cd ../examples/geral

echo Starting a test "client"

if [ $#  == 0 ] ; then
    echo It must print \".0\"
else
    echo It must print \".0\" $1 times 
fi


java -classpath $CLASSPATH TestAg $1
